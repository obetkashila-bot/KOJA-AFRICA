KOJA ALL-PLATFORM V1 - DESKTOP FOUNDATION

Electron wrapper targets:
- Windows: NSIS installer
- macOS: DMG
- Linux: AppImage

The wrapper loads https://koja-africa.onrender.com and uses the same KOJA backend, authentication and modules.

Build:
  npm install
  npm run build

Code signing and notarization are publisher-controlled and are intentionally not embedded.
