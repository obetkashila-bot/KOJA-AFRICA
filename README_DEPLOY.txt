KOJA GLOBAL BUSINESS V9 UI

This package updates the existing /services page into the KOJA Global Business operating center while preserving the existing Flask backend routes and Connect+ service.

Deploy to the existing Render service KOJA-AFRICA.
Run command remains: gunicorn app:app

No Supabase SQL migration is required for this UI-only release.

After deploy:
1. Open /health
2. Open /services
3. Confirm the new KOJA Global Business page appears.
